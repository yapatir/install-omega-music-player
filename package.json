{
  "name": "omega",
  "version": "1.3.0",
  "author": "yapatir",
  "description": "Omega Music Player - Offline Desktop App",
  "main": "electron/main.js",
  "homepage": "./",
  "scripts": {
    "start": "node scripts/set-version.js && react-scripts start",
    "build": "node scripts/set-version.js && react-scripts build",
    "electron": "electron .",
    "electron-dev": "node scripts/set-version.js && concurrently \"cross-env BROWSER=none react-scripts start\" \"wait-on http://localhost:3000 && electron .\"",
    "build-electron": "npm run build && electron-builder",
    "pack": "electron-builder --dir",
    "electron-build": "npm run build && npx electron .",
    "start-win": "set BROWSER=none && react-scripts start"
  },
  "dependencies": {
    "electron-updater": "^6.8.3",
    "lucide-react": "^0.383.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "cross-env": "^7.0.3",
    "electron": "^28.0.0",
    "electron-builder": "^24.6.0",
    "react-scripts": "5.0.1",
    "wait-on": "^7.2.0"
  },
  "build": {
    "appId": "com.omega.musicplayer",
    "productName": "Omega",
    "publish": [
      {
        "provider": "github",
        "owner": "yapatir",
        "repo": "install-omega-music-player"
      }
    ],
    "afterPack": "./scripts/stripLocales.js",
    "directories": {
      "buildResources": "public",
      "output": "dist"
    },
    "files": [
      "build/**/*",
      "electron/**/*",
      "!node_modules/**/*",
      "node_modules/**/*"
    ],
    "npmRebuild": false,
    "asar": true,
    "compression": "maximum",
    "extraMetadata": {
      "main": "electron/main.js"
    },
    "win": {
      "target": "nsis",
      "icon": "public/icon.png"
    },
    "mac": {
      "target": "dmg",
      "icon": "public/icon.png"
    },
    "linux": {
      "target": "AppImage",
      "icon": "public/icon.png"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true
    }
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
